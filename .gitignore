# Real config contains the IMAP password — never commit it
imap-scrub.yml

# Terraform
.terraform/
backend.hcl
*.tfstate
*.tfstate.*
*.tfvars

# Saved plan files can contain sensitive values
tf.plan
*.tfplan

# Claude Code session data (local settings, worktrees)
.claude/
